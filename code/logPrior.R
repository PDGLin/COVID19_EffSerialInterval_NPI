# logPrior function
logPrior <- function(parameters)
{
	return(0);
}


# # Prior setting
# logPrior <- function(parms, vecAlpha, vecBeta) {
#   logP = -log(vecBeta - vecAlpha);  # flat priors
#   return( sum(logP) );
# }